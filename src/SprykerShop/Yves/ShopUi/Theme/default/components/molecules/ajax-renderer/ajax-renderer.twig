{% extends model('component') %}

{% define config = {
    name: 'ajax-renderer',
    tag: 'ajax-renderer',
} %}

{% define attributes = {
    'provider-class-name': '',
    'target-class-name': '',
    'parent-class-name': '',
    'render-if-response-is-empty': false,
    'mount-after-render': false,
} %}
